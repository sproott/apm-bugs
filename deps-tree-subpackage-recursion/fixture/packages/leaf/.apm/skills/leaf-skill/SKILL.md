---
name: leaf-skill
description: Marker skill carried by the terminal leaf sub-package. Its
  appearance at every nesting level of `apm deps tree` is the visible symptom
  of the self-nesting bug.
---

# leaf-skill

Marker primitive. Present only so `fixture-leaf` is a valid APM package and so
the transitive skill shows up (duplicated) at each self-nested level.
