# Regenerated by `apm install`; never checked in.
consumer/apm_modules/
consumer/apm.lock.yaml
consumer/.claude/
consumer/.gitignore

# Isolated user-scope HOME used by the `user` target (keeps the real ~/.apm untouched).
fake-home/
